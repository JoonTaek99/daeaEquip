<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "//mybatis.org//DTD Config 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.daea">
	<insert id="insertData" parameterType="EquiDto">
        INSERT INTO TEST (id, model, manufacturer, operator, department, IP, acqdate)
        VALUES (#{id}, #{model}, #{manufacturer}, #{operator}, #{department}, #{IP}, #{acqdate})
    </insert>
    
    <select id="getAllData" resultType="EquiDto">
        SELECT * 
        FROM TEST
    </select>
    
    <select id="searchData" parameterType="String" resultType="EquiDto">
        SELECT *
        FROM TEST
        WHERE ID LIKE CONCAT('%' ,#{keyword}, '%')
    </select>
	
</mapper>
